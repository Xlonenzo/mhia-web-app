# Prevent platform-specific optional dependencies
omit=optional
no-optional=true

# Force resolution
force=true

# Ignore scripts during install (safer for cross-platform)
ignore-scripts=false

# Use exact versions
save-exact=false